import React, { useEffect, useState, useRef } from "react";


const InvImport = ({
    onChange
}) => {


    /*   const [value, setValue] = useState(initValue || "");
    const inputRef = useRef(null);   useEffect(() => {        setValue(initValue || "");    }, [initValue]);
    
        const handleInputChange = (event) => {
            const newValue = event.target.value;
            setValue(newValue);
    
            if (onChange) {
                onChange(newValue);
            }
        };
    
        */

    return (
        <div className="inventory_import_win">1. Download Adrenaline bot script here *LINK*</div>

    );
};

export default InvImport;